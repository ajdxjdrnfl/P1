syntax = "proto3";
package Protocol;

enum MoveState
{
	MOVE_STATE_NONE = 0;
	MOVE_STATE_IDLE = 1;
	MOVE_STATE_RUN = 2;
	MOVE_STATE_JUMP = 3;
	MOVE_STATE_SKILL = 4;
	MOVE_STATE_STUN = 5;
}

enum CasterType
{
	CASTER_TYPE_NONE = 0;
	CASTER_TYPE_WARRIOR = 1;
	CASTER_TYPE_MAGE = 2;
	CASTER_TYPE_BOSS = 3;
	CASTER_TYPE_MOB = 4;
}